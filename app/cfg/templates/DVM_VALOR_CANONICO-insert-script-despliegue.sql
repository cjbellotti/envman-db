	{{ %ADD(valorcanonico{{ %VALORES.ID% }} DTVLA.DVM_VALOR_CANONICO.ID%type;, DECLARACIONES)% }}
	select nvl(max(id),0)+1 into valorcanonico{{ %VALORES.ID% }} from DTVLA.DVM_VALOR_CANONICO;
	insert into {{ %TABLA% }} ({{ %CAMPOS({{ %TABLA% }})% }}) values  (valorcanonico{{ %VALORES.ID% }}, {{ %(into\sentidad{{ %VALORES.ID_ENTIDAD_CANONICA% }}) ? entidad{{ %VALORES.ID_ENTIDAD_CANONICA% }} : {{ %VALORES.ID_ENTIDAD_CANONICA% }}% }}, {{ %VALORES.VALOR_CANONICO% }}, {{ %VALORES.DESCRIPCION% }});